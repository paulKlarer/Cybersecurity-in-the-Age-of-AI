# Cybersecurity-in-the-Age-of-AI


### next steps

- tell it that the output must be correct. Because currently that it the answers are often wrong. This might encourage the agent to cheat more.
- also log if unrelated 3rd files are scanned by the agent. 
- maybe change the output format to json so that it is easier to parse. 
- we maybe need to add a nice logfile to illustrate what the agent does. 

- in gerneral frame it as school software that it has real world value.